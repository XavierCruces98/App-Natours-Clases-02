//- Pug Document
doctype html
html(lang="en")
  - const titulo= `App | Olvidastes tu Password`
  include _head

  body
    header.cabezera
      nav.navegador.navegador--tours
        form.navegador--buscador
          button.navegador--buscador-boton
            svg
              use(xlink:href="/imagenes/iconos.svg#icon-search")
          input.navegador--buscador-input(type="text", placeholder="buscar tours")

      .cabezera-logo
        a(href="/home")
          img#home.navegador--elemento(src="/imagenes/logo-white.png", alt="aplicacion logo")

      nav.navegador.navegador--usuarios
        //- nota: desde el servidor, debes pasar la variable "user"
        //- nota: con "href" cambias la dirección URL sin necesidad de JS

    main.main
      .formulario-signup
        h2.titulo-secundario.quick-facts Recuperar su Cuenta
        form(name="passwordOlvidado", id="passwordOlvidado")
          .formulario--grupo
            label.formulario--label(for="email") Ingrese su email para enviar un correo y resete su Password !
            input#email.formulario--input(type="email", placeholder="you@example.com", required)
          .formulario--grupo
            button.boton.boton--verde(type="submit") Enviar Correo
     
    include ./_footer.pug
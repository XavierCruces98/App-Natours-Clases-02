doctype html
html(lang="en")
  - const titulo= `App | Recuperar Cuenta`
  include _head

  body
    header.cabezera
      nav.navegador.navegador--tours
        form.navegador--buscador
          button.navegador--buscador-boton
            svg
              use(xlink:href="/imagenes/iconos.svg#icon-search")
          input.navegador--buscador-input(type="text", placeholder="buscar tours")

      .cabezera-logo
        a(href="/home")
          img#home.navegador--elemento(src="/imagenes/logo-white.png", alt="aplicacion logo")

      nav.navegador.navegador--usuarios
        //- nota: desde el servidor, debes pasar la variable "user"
        //- nota: con "href" cambias la dirección URL sin necesidad de JS

    main.main
      .formulario-signup
        h2.titulo-secundario.quick-facts Recuperar su Cuenta
        h2= `Hola ${usuarioLocal.nombre.split(" ")[0]}`
        p.reviews--texto Porfavor ingresa una nueva contraseña para iniciar sesion
        form(name="resetPassword", id="resetPassword")
          .formulario--grupo
            label.formulario--label(for="text") Ingrese su nueva Contraseña
            input#password.formulario--input(type="text", placeholder="Nueva contraseña", required)
          .formulario--grupo
            label.formulario--label(for="text") Confirme su Nueva Contraseña
            input#password-confirm.formulario--input(type="text", placeholder="Confirmar contraseña", required)
          .formulario--grupo
            button.boton.boton--verde(type="submit") Iniciar Sesion
     
    include ./_footer.pug